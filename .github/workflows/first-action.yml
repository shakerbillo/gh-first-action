name: First Workflow
on: workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    steps: 
      - name: Print greeting
        run: |
              echo "Hello Emmanuel"
              echo '"whats good?"
      - name: Print goodbye
        run: |
            echo "Done - bye!"
            echo "See ya!"


